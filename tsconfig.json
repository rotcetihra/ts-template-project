// tsconfig.json
//
// ----------------------------------------------------------------------------
//                                   ОПИСАНИЕ
// ----------------------------------------------------------------------------
// Конфигурация для среды разработки и IDE. Предназначена для проверки типов,
// навигации и подсветки синтаксиса, а также для запуска тестов.
// Генерация выходных файлов отключена (noEmit: true).
//
// Включает:
//   • sourceMap для отладки в IDE;
//   • явный импорт модулей с расширениями .ts/.mts (NodeNext);
//   • анализ исходников и тестов.
// Для сборки используйте tsconfig.build.json.
// ----------------------------------------------------------------------------
{
    // Наследование общих параметров из базовой конфигурации
    "extends": "./tsconfig.base.json",

    "compilerOptions": {
        // Ничего не писать на диск
        "noEmit": true,

        // Отладка в IDE
        "sourceMap": true,

        // Разрешить импорты вида "./file.ts"
        "allowImportingTsExtensions": true
    },

    // Проверяем и исходники, и тесты
    "include": ["src", "tests"],

    // Пропускаем сборку и зависимости
    "exclude": ["dist", "node_modules"]
}
