// src/classes/AbstractBase.ts

/**
 * Абстрактная база.
 *
 * @abstract
 * @category Абстракции
 */
abstract class AbstractBase {
    /**
     * Абстрактный метод.
     *
     * @abstract
     */
    abstract mustImplement(): void;
}

export default AbstractBase;
